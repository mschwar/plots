# CLAUDE.md

This file is kept for compatibility. The active operating guide lives in [AGENTS.md](AGENTS.md).

Notes:

- `build_all.py` is now runnable from the repo root.
- `model-sizes/` is not a no-src exception; it has `src/` generators and is built like the other plots.
- Use `python scripts/validate_repo.py --check` for the repo-level validation pass.
